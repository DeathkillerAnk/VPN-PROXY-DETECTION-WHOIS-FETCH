# Code On Steroids IP Forensic
SIH 2020 Grand finale

## Steps to run
- clone the repo
- go to backend folder and run `npm install` in terminal
- go to frontend folder and run `npm install` in terminal
- go to frontend folder and run `npm start`